build/
*.elf
*.bin
*.uf2
*.map
*.dis

# ...but keep the prebuilt, versioned release firmware
!firmware/*.uf2

# Burn-in logs (generated)
soak-logs/

# Session handoff notes (working state, not for the public repo)
HANDOFF.md

# Production flash/test batch logs (generated)
batch-*.csv
